# GPU launcher for tensor parallelism across 2 GPUs
# Uses vLLM V0 for SLURM compatibility
_target_: hydra_plugins.hydra_submitit_launcher.submitit_launcher.SlurmLauncher
submitit_folder: ${hydra.sweep.dir}/.submitit/%j
timeout_min: 14400
nodes: 1
tasks_per_node: 1
cpus_per_task: 16
mem_gb: ${runtime.job_memory_gb}
partition: pierson
array_parallelism: 1
gpus_per_node: 2
name: historical_norms
additional_parameters:
  gres: gpu:2
  wckey: ""
setup:
  - export HYDRA_FULL_ERROR=1
  - source ~/.bashrc
  - source /share/pierson/matt/UAIR/.venv/bin/activate
  - export PYTHONUNBUFFERED=1
  - export PYTHONPATH=/share/pierson/matt/UAIR:$PYTHONPATH
  # Debug output
  - echo "=== GPU sanity check ==="
  - echo "HOST=$(hostname) DATE=$(date)"
  - echo "CUDA_VISIBLE_DEVICES=$CUDA_VISIBLE_DEVICES SLURM_JOB_GPUS=$SLURM_JOB_GPUS SLURM_GPUS_ON_NODE=$SLURM_GPUS_ON_NODE"
  - nvidia-smi -L || true
  # W&B configuration
  - export WANDB_SILENT=true
  - export WANDB_DISABLE_SERVICE=true
  - if [ -n "$WANDB_GROUP" ]; then export WANDB_GROUP="$WANDB_GROUP"; fi
  # CRITICAL: Force vLLM V0 engine BEFORE Python starts
  # Must be set in shell env, not runtime_env, so it's read at import time
  - export VLLM_USE_V1=0
  - export TOKENIZERS_PARALLELISM=false
  # Local scratch for temp files
  - if [ -d /scratch ]; then export TMPDIR=/scratch/$USER; mkdir -p $TMPDIR; fi
