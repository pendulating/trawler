# Uses Hydra Submitit Slurm launcher
_target_: hydra_plugins.hydra_submitit_launcher.submitit_launcher.SlurmLauncher
submitit_folder: ${hydra.sweep.dir}/.submitit/%j
timeout_min: 480
nodes: 1
tasks_per_node: 1
cpus_per_task: 8
mem_gb: 64
partition: pierson
array_parallelism: 1
gpus_per_node: 2
name: E_rule_tuples
additional_parameters:
  gres: gpu:2
setup:
  - export HYDRA_FULL_ERROR=1
  - source ~/.bashrc
  - source activate /share/pierson/matt/conda/cillm-lora
  # CRITICAL: Force CUDA_VISIBLE_DEVICES to match SLURM's actual allocation
  # This prevents bashrc or prolog scripts from overriding with incorrect values
  - if [ -n "$SLURM_JOB_GPUS" ]; then export CUDA_VISIBLE_DEVICES=$SLURM_JOB_GPUS; fi
  - echo "=== GPU sanity (pre-job) ==="
  - echo "HOST=$(hostname) DATE=$(date)"
  - echo "CUDA_VISIBLE_DEVICES=$CUDA_VISIBLE_DEVICES SLURM_JOB_GPUS=$SLURM_JOB_GPUS SLURM_STEP_GPUS=$SLURM_STEP_GPUS SLURM_GPUS_ON_NODE=$SLURM_GPUS_ON_NODE"
  - nvidia-smi -L || true
  - python -c 'import torch; print("torch.cuda.is_available=", torch.cuda.is_available()); print("torch.cuda.device_count=", torch.cuda.device_count()); [print(f"torch device {i}=", torch.cuda.get_device_name(i)) for i in range(torch.cuda.device_count())]' || true
  - unset WANDB_DISABLED
  - export RULE_TUPLES_SILENT=1
  - export WANDB_SILENT=true

