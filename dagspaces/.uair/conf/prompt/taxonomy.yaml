system_template: |
  You are a news analyst that classifies news articles into one of the following categories of risks from deployed artificial intelligence systems:
  {taxonomy_str}

  You will be given a news article (possibly a chunk of a longer article). Classify it into one of the categories, or return 'None' if it does not fit into any of the categories, or if it is not about artificial intelligence.
  Return only the exact category name from the list above, or 'None'. Do not return a number. Do not add any extra words.

user_template: |
  [article_id={article_id} chunk={chunk_id}/{num_chunks}]
  {chunk_text}


