# @package training.grpo
# ─────────────────────────────────────────────────────────────────────────────
# m-series GRPO training config (the redesigned modular reward stack).
#
# Standalone by design: this file does NOT inherit from
# online_rground_external.yaml. That file is the keeper-frozen v9-ckpt100 config
# (byte-reproducible until camera-ready) and carries the legacy 6-weight reward
# keys the m-series deletes. Per wiki/grpo_redesign/optimizer.md (§"The preset")
# and migration.md (§"Old → new map"), the m-series is ADDITIVE code — a new
# training-config group that copies the optimizer preset verbatim and swaps the
# reward keys for the module schema below.
#
# Cell mechanics: a grid cell is fully defined by two keys — reward_auxiliaries
# and task_mix (wiki/grpo_redesign/ablation-protocol.md §"Cell mechanics").
# The sweep conf/sweep/grpo_m1_grid.yaml varies ONLY those two + experiment.name.
# The values here are the `full` cell (both auxiliaries, 0.7/0.3 mix).
#
# STATUS 2026-07-28: the module keys below are LIVE — consumed by
# ModularReward / build_modular_dataset / make_direct_chunk_gold. This file is
# the wave-2 preset (post-m1-post-mortem): SFT-aligned rollout prompt,
# chunk-denominator R-DIRECT (restricted index, k=1 gold, tau=0.55),
# re-anchored batteries with the both-sides floor. The m1 values live in git
# history; m1 metrics are NOT comparable to wave-2 metrics (new scales).
# ─────────────────────────────────────────────────────────────────────────────

# ══ OPTIMIZER PRESET — copied VERBATIM from the keeper (online_rground_external
# ══ .yaml), fixed across every m-series grid cell. Source: optimizer.md §"The
# ══ preset". Every value below was set by a falsified alternative, not taste.

# Master RNG seed — seeds Python/NumPy/torch and is passed to GRPOConfig
# (seed + data_seed). The 5-seed protocol varies ONLY this. optimizer.md.
seed: 42

# G: G=2 gave 60% tied groups (May sweep). optimizer.md.
num_generations: 8
# lr 2e-5 with a cosine floor at 30% of peak — the default cosine decayed lr to
# ~0 before the policy moved (v5/v6). optimizer.md.
learning_rate: 2.0e-5
lr_scheduler_type: cosine_with_min_lr
min_lr_rate: 0.3
warmup_ratio: 0.1
# 32 completions = 4 prompts/step at G=8. Keeper batch economics.
per_device_batch_size: 1
gradient_accumulation_steps: 32
# 772 prompts x 1 epoch was too little signal. optimizer.md.
num_epochs: 3
max_completion_length: 3072
gradient_checkpointing: true
bf16: true
logging_steps: 10
save_strategy: steps
# Pre-breakout checkpoint cadence; held-out behaviour peaks early. optimizer.md.
save_steps: 50

# KL-to-SFT-ref coefficient. beta=0 + hot lr → entropy runaway 10x, IS collapse
# (v6); beta=0.02 fixed it single-variable (v7). A stability floor, not an
# anchor. optimizer.md.
beta: 0.02
# DAPO clip-higher. Inert at mu=1 (ratio ≡ 1) but kept so a mu change activates
# it without a second edit. optimizer.md.
epsilon_high: 0.28
# GRPO inner-iteration count mu. mu=2 moved held-out metrics but broke entropy
# ~step 240 (v8); reverted to 1 (v9). optimizer.md §"Relation to the v13a arm".
num_iterations: 1
# THE v1–v4 binding constraint: default sequence_mask zeroed exactly the long
# extraction gradients; token_truncate clamps per-token instead. optimizer.md.
vllm_importance_sampling_mode: token_truncate
# Dr. GRPO: group-std scaling amplifies near-tie judge noise. optimizer.md.
scale_rewards: "none"
# DAPO: truncated output fails JSON parse and would reward the short no-flow
# path. optimizer.md.
mask_truncated_completions: true
# loss_type: unset — TRL default "dapo" (no per-response length normalization).
# optimizer.md lists it as "(TRL default, unset)", so it is deliberately absent.
# Rollout temperature 1.0 is the TRL default (grpo_training.py forwards no
# temperature knob); the keeper relies on that default. optimizer.md lists
# "T=1.0" as the preset value. See DESIGN NOTES re the thinking-flag wording.

# vLLM for the policy model — colocate, 1-GPU cell economics. optimizer.md.
use_vllm: true
vllm_mode: colocate
vllm_tensor_parallel_size: 1
# 0.45 (NOT the code default 0.3): at gpu_memory_utilization=0.45 (colocated
# with optimizer+grads) vLLM's KV-cache pre-check fails on qwen3.5-9b unless
# max_model_length is capped — the keeper's documented value. See DESIGN NOTES:
# optimizer.md's table prose says "gpu_mem 0.3"; the keeper yaml sets 0.45.
vllm_gpu_memory_utilization: 0.45
vllm_enable_sleep_mode: true
# Matches eval-time engine_kwargs.max_model_len for qwen3.5-9b/*.yaml. Keeper.
vllm_max_model_length: 16384

# Thinking during GRPO generation. Keeper value = false (stripped pre-scoring).
# DESIGN NOTES: optimizer.md's table annotates rollout temperature as "thinking
# on, stripped pre-scoring" — a wording disagreement with the keeper's
# enable_thinking_grpo: false. Copied verbatim from the keeper (false).
enable_thinking_grpo: false

# Held-out dev split. DISABLED (0.0) for colocate runs — the in-loop eval's
# fp32 full-vocab logits OOM against the resident colocate vLLM engine. Keeper.
dev_fraction: 0.0
eval_steps: 50

# ══ M-SERIES MODULE KEYS — the modular reward stack (all LIVE; see STATUS).

# Composition selector: the new modular stack (gate → routing → outcome +
# auxiliaries with the 2:1 weight rule), NOT the v-era "directional" 6-weight
# path. Source: migration.md §"Old → new map" (CompositeRewardFunction →
# ModularReward).
reward_composition: modular

# Active auxiliaries — subset of {ground, contrast}. THE cell key (with
# task_mix). The 2:1 weight rule (outcome core = 2x each active auxiliary,
# auxiliaries equal, normalized to 1) is DERIVED from this list, not tuned.
# Source: ablation-protocol.md §"Cell mechanics" / §"Weights — resolved".
reward_auxiliaries: [ground, contrast]

# Task mix — extraction vs judgment-vignette prompt share. THE second cell key
# (−vignette cell sets {extract: 1.0, vignette: 0.0}). Renames the v-era
# vignette_ratio. Source: ablation-protocol.md §"Cell mechanics";
# migration.md (vignette_ratio → task_mix.vignette).
# REVISED 2026-07-28 (pre-launch audit): vignette 0.3 at target_n 600 needs
# 180 batteries, but both-sides batteries have a HARD data ceiling of ~115
# (230 minority-side eligible norms / 2) and the realized floor=1 pool is
# ~110. The m1 "0.3" was only achievable because 42.8% of its batteries were
# one-sided. 0.18*600 = 108 <= pool; the build now RAISES if the realized
# mix deviates from this by more than task_mix_tolerance.
task_mix: {extract: 0.82, vignette: 0.18}
task_mix_tolerance: 0.03

# Vignette battery construction. UPDATED 2026-07-28 (m1 post-mortem R3, then
# revised by the pre-launch audit): the floor is a build INVARIANT — clusters
# (and remainders) that cannot field `minority_floor` minority items are
# DROPPED, never emitted one-sided (42.8% of m1 batteries were one-sided and
# untestable for side discrimination). floor=1 (not 2): the corpus has only
# 230 minority-side eligible norms TOTAL, so floor=2 caps the pool at ~86
# batteries vs ~110 at floor=1 — minority scarcity is spent more efficiently
# at >=1/battery, and minority_target still pulls 2 when available.
battery: {k: 8, min_k: 4, minority_floor: 1, minority_target: 2}

# Probe pool + sampling. LEGACY-POPULATION PIN (2026-07-28): with the outcome
# core retired, probes are no longer scored by anything — but the build still
# attaches them and still EXCLUDES flow-bearing chunks with an empty pool,
# which is what defines the ~420-chunk extract population every wave-2
# calibration was measured on (chunk-gold index, tau, teacher-flow stats).
# Keep unchanged so the population stays pinned; probes also feed the
# direction-consistency diagnostic. Source: reward-outcome.md (retired);
# reward-direct-spec.md "Chunk-denominator R-DIRECT".
probes:
  pools_path: outputs/2026-07-23_mseries_premeasure/probe_calibration/probe_pools_filtered.parquet
  k_max: 4
  p_null: 0.8

# The frozen-answerer client was REMOVED 2026-08-12 with the outcome core.
# This node survives because aux_scorers._resolve_judge_url still reads it as
# the base-URL fallback for the listwise judge when no `aux_judge` node is set —
# which is the case here. Renaming or deleting it changes judge URL resolution.
# `model` MUST equal the served model id vLLM was launched with.
answerer: {base_url_env: VLLM_SERVER_URL, model: "${judge_model.model_source}"}

# R-GROUND / R-CONTRAST retrieval breadth. reward-ground.md / reward-contrast.md
# specify k=3 norms; pinned here (the code default is 5, the keeper listwise
# lineage value). Set to match the redesign spec (2026-07-24 review); reversible.
rank_top_k: 3
# Rank/grounding blend inside the listwise judge score (code default 0.5,
# pinned for the protocol record — its sibling rank_top_k already was).
rank_weight: 0.5

# Verifiable core (reward-direct-spec.md, 2026-07-25). "direct" = R-DIRECT: the
# governing norm classifies each extracted flow (force x act_polarity) and the
# reward is agreement with the policy's own appropriateness label — no answerer,
# no judge, no server call. "outcome" (the frozen-answerer core) was removed
# 2026-08-12 and is now rejected: handed the prohibiting norm verbatim it still
# called ~83% of prohibited flows appropriate, for +0.042 macro of learnable
# signal. That negative result is recorded in wiki/grpo_redesign/reward-outcome.md.
core_mode: direct

# ══ WAVE-2 R-DIRECT KEYS (2026-07-28, m1 post-mortem R1/R2 — see
# ══ reward-direct-spec.md "CORRECTION 2026-07-28" + "Chunk-denominator").
# ══ All four match the code defaults; pinned here so the sweep yaml is the
# ══ complete protocol record.

# R1: rollout prompts byte-identical to the SFT training instruction
# (sft_data_prep.sft_aligned_extract_template — imported builder, never a
# config copy). The m1 wave scored the policy on conf/prompt/ci_extraction
# .yaml, a prompt it never trained on: 34.4% vs 2.7% R-VALID gate failure
# (A/B probe, outputs/2026-07-28_ab_prompt_probe). false = m1 behaviour.
sft_aligned_extract_prompt: true

# R2: chunk-fixed denominator. The completion is matched against the CHUNK's
# teacher flows (gold precomputed at build; misses cost recall) instead of
# macro-averaging over its own extracted flows — per-completion macro
# collapsed to plain accuracy on 50.7% of m1 scored completions (reward 0.73
# at balanced accuracy 0.56). false = m1 per-flow behaviour.
direct_chunk_denominator: true

# Gold per teacher flow: top-1 over the restricted index (k=1). REVERTED from
# k=3 on 2026-07-28 audit evidence: a k=3 majority vote regresses gold toward
# the index's 72.5/27.5 class prior — measured on the real wave-2 population
# it deleted 19% of minority-class golds, flipped 15% of all golds (206
# inappr->appr vs 117 reverse), and cut mixed-gold chunks 68.6% -> 56.1%.
# k=1 matches the measured acceptance bar (teacher macro 0.482,
# reward-direct-spec.md) and the pre-registered ~68% mixed-chunk expectation.
direct_gold_k: 1

# Greedy 1:1 match threshold, calibrated 2026-07-28 on 2,518 policy flows
# under the aligned prompt (outputs/2026-07-28_tau_calibration): separation
# plateaus at tau 0.55-0.575 (J=0.70); 0.55 = the signal-preserving end
# (87.5% true-match retention / 17.5% null acceptance).
direct_match_threshold: 0.55

# Retrieval artifacts for the chunk-gold index + R-DIRECT retriever. Both are
# canonical server.env values (stage jobs re-read server.env; launch-shell
# exports do NOT cross submitit). embeddings_dir rows are 1:1 with the
# UNFILTERED per-book norm lists — NormRetriever masks both together and
# refuses loudly on misalignment.
extraction_path: ${oc.env:CI_EXTRACTION_PATH}
embeddings_dir: ${oc.env:NORM_EMBEDDINGS_PATH}

# A-ABSTAIN reward table (extended to gold-NO extractions). Mirrors the keeper's
# no_flow_reward(gold): gold=YES→wrong 0.1, gold=NO→correct 0.6, unknown→0.4.
# Source: reward-abstain.md; migration.md (abstention_score → A-ABSTAIN).
abstain: {wrong: 0.1, correct: 0.6, unknown: 0.4}

# Stratified prescreen. formula_version=m1 is a new cache namespace (no collision
# with the v-era keys); the module list + task_mix are also part of the cache
# key, so every cell gets its own entries. target_n is the pre-registered
# prompt-set size selected by the stratified screen from the 2,993-chunk pool.
# The docs pre-register a RANGE (N ≈ 500–800, data.md §"Splits"; not pinned to a
# single value in ablation-protocol.md), so 600 is chosen here — N=600 is inside
# the range and near the 500-needs-150-batteries feasibility point (data.md
# §"Battery feasibility"). Source: prescreen-and-gates.md §"Pre-registered N".
prescreen:
  formula_version: m1
  target_n: 600
