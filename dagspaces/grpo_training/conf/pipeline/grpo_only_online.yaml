# @package _global_
# GRPO-only pipeline with online R_ground evaluation.
# Assumes SFT checkpoint, norm universes, and norm embeddings exist.
# Does NOT require reward_cache (R_ground is evaluated live).
#
# Usage:
#   SFT_CHECKPOINT_PATH=... NORM_UNIVERSES_PATH=... NORM_EMBEDDINGS_PATH=... \
#   SFT_PAIRS_PATH=... \
#   python -m dagspaces.grpo_training.cli \
#     pipeline=grpo_only_online \
#     training/grpo=online_rground_5gpu \
#     model=qwen3-8b

defaults:
  - override /hydra/launcher: slurm_monitor

pipeline:
  output_root: ${hydra:run.dir}/grpo_only_online

  sources:
    sft_checkpoint:
      path: ${oc.env:SFT_CHECKPOINT_PATH}
      type: dir
    norm_universes:
      path: ${oc.env:NORM_UNIVERSES_PATH}
      type: json
    norm_embeddings:
      path: ${oc.env:NORM_EMBEDDINGS_PATH}
      type: dir
    sft_pairs:
      path: ${oc.env:SFT_PAIRS_PATH}
      type: parquet

  graph:
    nodes:
      grpo_training:
        stage: grpo_training
        depends_on: []
        inputs:
          sft_checkpoint: sft_checkpoint
          dataset: sft_pairs
          norm_universes: norm_universes
          embeddings: norm_embeddings
        outputs:
          checkpoint:
            path: outputs/grpo/checkpoint
            type: dir
        launcher: slurm_gpu_5x
        wandb_suffix: grpo_training
