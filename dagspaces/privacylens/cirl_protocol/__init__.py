"""PrivacyLens evaluated under the CIRL protocol (ported from the retired
cirl_vignettes dagspace). (A)/(B) rejection probing + naive/privacy_enhanced
trajectory leakage on the PrivacyLens-493 data vendored in the CI-RL repo.

This is the *protocol* comparison — same underlying PrivacyLens cases as the
native privacylens pipelines, but with the CIRL paper prompt framing.
"""
