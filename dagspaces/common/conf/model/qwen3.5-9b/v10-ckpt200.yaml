# @package _global_
# v10 GRPO — checkpoint-200 adapter (epoch ~1.16), the later-training counterpart
# to v10-ckpt100. Same cost-sensitive directional reward
# (rground_app_floor_prohibit=0.1); this config only advances the LoRA from
# checkpoint-100 to checkpoint-200 to test whether the held-out Forbid-recall /
# compliance-F1 picture improves with more training (ckpt-100 had not yet beaten
# v9 on the matched GoldCoin sweep). Base merged_sft + all engine settings are
# identical to v10-ckpt100 so the comparison is checkpoint-only.
# Source run: multirun/2026-06-24_grpo_redesign_full_v10/20-48-28 (job 874897).
# See wiki/grpo_training_field_notes/2026-06-24_v10_plan.md.
model:
  model_source: /share/pierson/matt/UAIR/multirun/2026-06-24_grpo_redesign_full_v10/20-48-28/grpo_only_online_external/outputs/grpo/checkpoint/_merged_sft
  model_family: qwen3.5
  lora_path: /share/pierson/matt/UAIR/multirun/2026-06-24_grpo_redesign_full_v10/20-48-28/grpo_only_online_external/outputs/grpo/checkpoint/checkpoint-200
  thinking_mode: off
  chat_template_kwargs:
    enable_thinking: false
  engine_kwargs:
    max_model_len: 16384
    max_num_seqs: 16
    tensor_parallel_size: 1
    trust_remote_code: true
    enforce_eager: true
    enable_lora: true
    max_lora_rank: 64
  batch_size: 0
  concurrency: 1
