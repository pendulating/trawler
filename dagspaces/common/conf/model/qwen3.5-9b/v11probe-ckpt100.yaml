# @package _global_
# v11 probe (top100 vignettes) — checkpoint-100 adapter (epoch ~0.57). Part of the
# held-out GoldCoin/ConfAIDE sweep for the v11 lever-(a) probe: judgment vignettes
# drawn from the top100 universe (realised 2.15:1 yes:no vs v10's 5.2:1), all else
# v10-identical (cost-sensitive rground_app_floor_prohibit=0.1). Early checkpoint —
# the v8/v9/v10 pattern peaks before epoch ~1.2. Base merged_sft + engine settings
# identical across v11probe-ckpt* so the comparison is checkpoint-only.
# Source run: multirun/2026-06-30_grpo_probe_top100_vignettes/11-53-47 (job 488187).
# See wiki/grpo_training_field_notes/2026-07-01_v11_probe_midrun_forensics.md.
model:
  model_source: /share/pierson/matt/UAIR/multirun/2026-06-30_grpo_probe_top100_vignettes/11-53-47/grpo_only_online_external/outputs/grpo/checkpoint/_merged_sft
  model_family: qwen3.5
  lora_path: /share/pierson/matt/UAIR/multirun/2026-06-30_grpo_probe_top100_vignettes/11-53-47/grpo_only_online_external/outputs/grpo/checkpoint/checkpoint-100
  thinking_mode: off
  chat_template_kwargs:
    enable_thinking: false
  engine_kwargs:
    max_model_len: 16384
    max_num_seqs: 16
    tensor_parallel_size: 1
    trust_remote_code: true
    enforce_eager: true
    enable_lora: true
    max_lora_rank: 64
  batch_size: 0
  concurrency: 1
